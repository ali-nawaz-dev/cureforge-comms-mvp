"""
Comms Agent package — wraps the Communication-AI-Agent (Layer 3A base).

Source repo: github.com/ali-nawaz-dev/cureforge-comms-mvp
Integration step:
  1. Clone / copy the repo contents into this directory.
  2. Expose the outreach sender surface via:
       from packages.comms_agent import OutreachDraftingService
  3. services/outreach/drafting.py already implements the MVP-shim;
     swap the import once the real code is verified against tests.
  4. Remove this stub comment once real code is in place.
"""
